export const SoundStyles = /* css */ `
.sound {
  display: flex;
  align-items: center;
  padding: 3px 7px;
  background-color: var(--blue);
  border-radius: 2px;
  box-shadow: var(--light-shadow);
}
.simple-bar--no-color-in-data .sound {
  background-color: var(--minor);
}
.sound__icon {
  width: 14px;
  height: 14px;
  margin-right: 4px;
  fill: var(--main);
}
.simple-bar--no-color-in-data .sound__icon {
  fill: #fff;
}
`
