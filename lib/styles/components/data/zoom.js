export const ZoomStyles = /* css */ `
.zoom {
  position: relative;
  display: flex;
  align-items: center;
  padding: 3px 7px;
  background-color: var(--minor);
  border-radius: 2px;
  box-shadow: var(--light-shadow);
}
.zoom__icon {
  width: 14px;
  height: 14px;
  fill: var(--main);
  margin: 0 1px 0 4px;
}
.zoom__icon--on {
  fill: var(--red);
}
`
