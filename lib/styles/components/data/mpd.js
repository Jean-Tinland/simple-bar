export const mpdStyles = /* css */ `
.mpd {
  position: relative;
  background-color: var(--cyan);
}
.mpd__inner {
  max-width: 140px;
  display: flex;
  flew-wrap: nowrap;
  overflow: hidden;
}
.mpd__slider {
  white-space: nowrap;
  transition: transform 160ms var(--transition-easing);
}
`
