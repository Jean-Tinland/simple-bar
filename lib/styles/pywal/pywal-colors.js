import { pywal } from './pywal'

export const pywalColors = {
  main: pywal.background,
  mainAlt: pywal.function,
  minor: pywal.function,
  accent: pywal.result,
  red: pywal.operator,
  green: pywal.result,
  yellow: pywal.cursor,
  orange: pywal.scrollbar,
  blue: pywal.matched,
  magenta: pywal.variable,
  cyan: pywal.function
}
